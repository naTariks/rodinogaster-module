if SHIELD_RODINOGASTER_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Pink Robin"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_RODINOGASTER_LEFT || SHIELD_RODINOGASTER_RIGHT

config ZMK_SPLIT
    default y

endif
